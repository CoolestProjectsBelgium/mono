# Voting Application Environment Variables

# API base URL for voting endpoints (used at build time for static generate)
NUXT_PUBLIC_API_BASE_URL=https://api.coolestprojects.localhost:8443

# Port-forward local dev: open http://localhost:3005 — api-base resolves to http://localhost:3001
