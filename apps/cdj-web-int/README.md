# @coolestprojects/cdj-web-int

Archived Coolest Projects Belgium project galleries (offline snapshot for Level27 `static-prod`).

**Documentation:** [docs/apps/cdj-web-int.md](../../docs/apps/cdj-web-int.md)

```bash
npm run archive-cpbe
npm run start:dev --workspace=apps/cdj-web-int
```

Participant photos under `images/` are gitignored. Run `archive-cpbe` before deploy.
