{
  "name": "coolestprojects",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:debug": "npm run start:debug -w @coolestprojects/api",
    "start:dev": "npm run start:dev -w @coolestprojects/api",
    "build": "npm run build -w @coolestprojects/api"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jest": "^30.0.0",
    "@types/mocha": "^10.0.10"
  }
}
